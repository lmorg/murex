// Parser for the murex shell scripting language
//
// The core functions you will need to be aware of are `ProcessNewBlock` for executing a new murex process, and
// `ParseBlock` for when you only require validation of the correctness of the shell script's syntax.
package lang
