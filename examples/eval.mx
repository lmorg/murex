#!/usr/bin/env murex

let a=0
while { eval a < 5 } {
    out "Iteration $a...."
    let a++
}

# This can also be written using a more traditional syntax:
# for { a=0; a<5; a++ } {
#   out "Iteration $a...."
# }