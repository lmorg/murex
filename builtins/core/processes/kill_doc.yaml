- DocumentID: fid-kill
  Title: >+
    Kill Function (`fid-kill`)
  CategoryID: commands
  SubCategoryIDs:
    - commands.proc
  Summary: >-
    Terminate a running Murex function
  Description: |-
    `fid-kill` will terminate a running Murex function in a similar way
    that the POSIX `kill` (superficially speaking).
  Usage: |-
    ```
    fid-kill fid
    ```
  Examples:
  Flags:
  Detail: |-
    {{ include "gen/includes/fid-kill.inc.md" }}
  Synonyms:
    - fid-kill
  Related:
    - fid-list
    - fid-killall
    - bg
    - fg
    - jobs
    - exec
    - murex-update-exe-list
    - builtins
    - bexists
    - fexec



- DocumentID: fid-killall
  Title: >+
    Kill All In Session (`fid-killall`)
  CategoryID: commands
  SubCategoryIDs:
    - commands.proc
  Summary: >-
    Terminate all running Murex functions in current session
  Description: |-
    `fid-killall` will terminate **all** processes and Murex functions in the
    current session.
  Usage: |-
    ```
    fid-killall
    ```
  Examples:
  Flags:
  Detail: |-
    `fid-killall` works by the same mechanisms as `fid-kill`, described below:

    {{ include "gen/includes/fid-kill.inc.md" }}
  Synonyms:
    - fid-killall
  Related:
    - fid-list
    - fid-kill
    - bg
    - fg
    - jobs
    - exec
    - fexec
    - murex-update-exe-list
    - builtins
    - bexists
