- DocumentID: "autocomplete"
  Title: >+
    Tab Autocompletion (`autocomplete`)
  CategoryID: commands
  SubCategoryIDs: [ commands.shell ]
  Summary: >-
    Set definitions for tab-completion in the command line
  Description: |-
    `autocomplete` digests a JSON schema and uses that to define the tab-
    completion rules for suggestions in the interactive command line.
  Usage: |-
    ```
    autocomplete get [ command ] -> <stdout>

    autocomplete set command { mxjson }
    ```
  Examples:
  Detail: |-
    ### Undefining autocomplete

    Currently there is no support for undefining an autocompletion rule however
    you can overwrite existing rules.

    ## Directives

    {{ include "builtins/core/autocomplete/autocomplete_doc.inc.md" }}
  Flags:
    get: output all autocompletion schemas
    set: define a new autocompletion schema
  Synonyms:
    - autocomplete
  Related:
    - config
    - private
    - function
    - alias
    - summary
    - mxjson
    - item-index
    - get-type
    - switch
    - stdin
